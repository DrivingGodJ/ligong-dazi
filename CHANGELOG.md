# 更新日志

## 2026-09-25 · 邀请偏好与学号身份申诉

- “允许被邀请”关闭后，用户不会再出现在找搭子的人选结果中；即使发起人保留了旧的匹配页面，确认邀请时也会再次拦截。
- 学号占用申诉改为必须上传学生卡人像面并留下联系方式；学生卡不会公开，也不放进网页静态目录。
- 审核 Agent 只初审学校、图片清晰度和学号是否一致。模型不可用、置信度不足或材料有疑点时转人工，不会直接冻结或交接账号。
- 申诉人材料通过后，原账号冻结 24 小时，期间无法使用且不会参与匹配；原账号可上传自己的学生卡或主动放弃账号。
- 原账号逾期、主动放弃或材料未通过时，学号会交给申诉人创建的新账号；原账号的活动、评价、信用和画像历史不会随学号转移。
- 双方材料均通过初审时，页面要求原账号补充联系方式，并在后台集中展示双方材料和联系方式，由管理员决定保留原账号或交给申诉人。

## 2026-09-22 · 学号注册与申诉

- 新账号改用学号注册和登录，成功后直接进入本校区活动广场；学号不公开，也不等于已完成学籍认证。
- 原邮箱注册的账号可继续用邮箱和密码登录，收到一次性消息并在画像页提醒补填学号；学号绑定后可用学号登录，不能自行改绑。
- 注册或旧账号补填时若学号已占用，可保留当前输入并提交联系方式申诉。后台提供待处理、已处理列表和人工处理标记；标记本身不会自动更改账号归属。

## 2026-09-22 · 大更新 2：本校区入局与消息提醒

- 校区严格限定南京、江阴；旧账号若校区无法识别，先在画像页补选。活动广场、匹配与新活动加入限制在本校区；已有共同活动仍可查阅。
- 主界面五个功能标签移至底部固定导航，留出顶部消息入口，并适配手机安全区域。
- 发起活动可选择公开自由加入，或申请后由当前每一位成员审核；审核前可以查看申请人档案，少一人同意都不会正式入局。满员后仍等待的申请会关闭并提醒。
- 增加站内消息和可选手机推送：新邀请、活动开始前 15 分钟、申请加入、有人加入公开活动、改期提案；消息可查看、标记已读。
- iOS 注册提示 Safari 添加到主屏幕；安卓提供可验证签名的 Trusted Web Activity 安装包与版本更新提示。推送需用户授权，下载更新需用户确认安装。

## 2026-09-22 · 更灵活地找活动

- 找搭子时地点改为选填；地点空着也能搜索用户和已有活动，但确认创建新活动或单人活动时必须补填。直接创建活动同样拒绝空白地点。
- “还需要几人”改成 1—9 人滑块，与画像人数偏好的操作方式一致；活动总人数包含发起人。
- “其他活动”与预设类型并列展示，选中后才展开名称输入框。
- 同类已有活动的开始时间允许与计划前后相差最多 2 小时；相近时段会按时间差降分，卡片显示实际开始和结束时间，加入前再次提示。用户候选的时间冲突规则不变。

## 2026-09-22 · 校区与手机端体验

- 注册与个人画像的校区改为南京、江阴二选一；启动时自动迁移能识别的旧校区写法，无法确定的资料由用户在画像页选择。
- 画像改为自动保存，人数偏好改用滑块；性别选择改为带文字和符号的按钮，修复手机上原生下拉框的符号错位。
- 修正手机端导航和日期时间输入框的宽度，避免内容越出卡片；退出登录移到画像页最底部。
- 创建活动可选择“只允许同性加入”；匹配候选、邀请接受及活动广场加入均执行限制，勾选后不会误加入其他人创建的普通活动。
- 活动广场只保留名称／地点搜索、现有活动类型和日期筛选，固定按适合度排序，并继续支持分批加载。
- 新增对应的校区迁移、同性限制和广场筛选测试。

## 2026-09-19 · 开发比赛版本

昨晚完成了「理工搭子局」从可运行 MVP 到完整体验版本的一轮集中迭代，覆盖 Agent 匹配、用户画像、活动协作、会后评价和管理后台。

### 找搭子与 Agent

- 完成 FastAPI、SQLite/PostgreSQL 可替换数据层和 OpenAI 兼容 Agent 的基础架构。
- Agent 可以查询已有活动、查询时间合适的用户、计算匹配度，并给出可解释的推荐理由。
- 用户可以用自然语言补充个性化需求，例如“我是小白，希望大佬带”；敏感属性不会参与自动排序。
- 地点支持模糊匹配，可以识别“南体”“南区体育馆”等不同写法。
- 找搭子增加 60 秒冷却，避免重复提交。
- 模型拒答或返回损坏结果时，页面会退出运行状态、清空本次内容并允许立即重试，不再把异常结果伪装成成功匹配。

### 用户画像与个人主页

- 注册流程加入校区、院系、年级、性别、兴趣、常去地点和相处方式引导。
- 性别只允许选择“男、女、不公开”，并以颜色、符号和文字共同展示；性别不参与 Agent 自动排序。
- 用户可以添加多项爱好或特长，并用滑块选择“小白—精通”五档水平。
- 新增个人主页，可查看用户主动填写的资料，以及系统根据活动和评价整理出的综合表现。
- 新增“AI 看见的综合形象”，每 5 分钟可重新生成一次，只向本人展示。

### 活动广场与活动协作

- 新增活动广场，可按活动类型、地点和开始时间筛选，并支持分批加载和直接加入。
- 广场的“适合你”排序只使用兴趣、地点和人数偏好，不调用模型。
- 匹配结束后可以发布只有发起人的活动，等待其他用户从广场加入。
- 单人活动取消后会直接从活动库删除；多人活动只有在开始前 1 小时内取消才扣信用分。
- 活动卡片显示男、女和不公开人数，可继续查看参与者及其个人主页。
- 每位参与者都可以发起改期投票，其他参与者全部同意后才会修改活动时间。
- 新增手机日历文件导出。
- 活动开始前 15 分钟开放集合照片；照片只对同场成员可见、只保留最新 5 张，并在活动结束后自动删除。

### 会后评价与信用体系

- 活动结束后开放评分、文字评价、性格标签和履约情况评价；到场情况只分为“正常参加”和“没有出现”。
- 评价审核会结合多人反馈、历史活动和既往评价判断真实性与恶意风险。
- 高影响或互相矛盾的评价会交给同场第三人复核，再决定信用分变化。
- 可以评价搭子的爱好水平；用户只填写实际观察到的水平，累计至少 3 条有效反馈后，系统才可能自动标注水平争议或疑似高手低报。
- 评价页醒目提醒用户务必如实填写，不确定的内容不要猜测。
- 活动结束后自动整理活动偏好、社交表现和履约记录，供后续匹配与风险判断使用。

### 界面、管理与工程质量

- 重做用户端界面和交互文案，补齐注册、找搭子、活动广场、我的活动、个人画像及各种弹窗状态。
- 管理后台支持选择 AI 服务商、填写本地 API Key，并用非技术语言展示运行日志。
- 增加本地演示数据、数据库兼容升级、照片清理任务和更完整的 README/协作说明。
- 自动测试覆盖注册画像、匹配、冷却、广场、取消规则、改期投票、集合照片、日历导出、评价复核、性别限制和越权拦截。

### 验证结果

- Python 代码规范检查通过。
- 前端 JavaScript 语法检查通过。
- 14 项自动测试全部通过。
- 已在真实浏览器中验证 Agent 失败恢复、性别选项、活动人数标签和核心页面。
