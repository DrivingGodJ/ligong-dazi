<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Getui still requires compatibility with an HTTP transport on some Android 9 devices.
         WebView itself keeps mixed content disabled and only opens our HTTPS origin. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
